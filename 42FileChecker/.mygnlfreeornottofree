The purpose of this test is to check wether get_next_line lets the caller freeing the lines itself after each call or not.
The subject does not tell you how to implement the memory management, so that both behaviors can be justified.
Result: The implementation of this get_next_line looks like `getline(3)`, the caller should free the line after each call.
